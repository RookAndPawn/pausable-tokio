# The submodule has its own .gitignore (target/, Cargo.lock, etc.); we
# only need rules for stuff we might create at the parent-repo level.

# editor files
.vscode/
.idea/
*.swp
*.swo

# macOS
.DS_Store
